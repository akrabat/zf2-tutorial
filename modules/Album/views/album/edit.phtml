<?php
$title = 'Edit album';
$this->title = $title; // assign to $this to use in layout.phtml
?>
<h1><?php echo $this->vars()->escape($title); ?></h1>

<?php
$form->setAction($this->url(array('controller' => 'album', 'action' => 'edit'), 
        array('name' => 'default')))
     ->setView($this);
echo $form;

?>
